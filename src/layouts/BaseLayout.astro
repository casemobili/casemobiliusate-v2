---
import '~/styles/global.css';
import Header from '~/components/Header.astro';
import Footer from '~/components/Footer.astro';
import SchemaOrganization from '~/components/SchemaOrganization.astro';
import PillarHero from '~/components/PillarHero.astro';
import { SITE } from '~/lib/site';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  noindex?: boolean;
  ogType?: 'website' | 'article';
  hideHero?: boolean;
}

const {
  title = SITE.defaultTitle,
  description = SITE.defaultDescription,
  image = '/og-default.jpg',
  noindex = false,
  ogType = 'website',
  hideHero = false,
} = Astro.props;

const path = Astro.url.pathname;
const segments = path.split('/').filter(Boolean);
const firstSegment = segments[0] ?? '';
const isHomePage = path === '/' || path === '';
const showHero = !hideHero && !isHomePage && firstSegment.length > 0;
const pillarSlug = firstSegment;

const fullTitle = title === SITE.defaultTitle ? title : `${title} | ${SITE.name}`;
const canonical = new URL(Astro.url.pathname, SITE.url).toString();
const ogImage = image.startsWith('http') ? image : `${SITE.url}${image}`;
---

<!doctype html>
<html lang={SITE.language}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    {noindex && <meta name="robots" content="noindex,nofollow" />}

    <meta property="og:type" content={ogType} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:site_name" content={SITE.name} />
    <meta property="og:locale" content={SITE.locale} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <!-- Favicon e icons (multi-platform) -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/manifest.webmanifest" />
    <meta name="theme-color" content="#1F2A1B" />
    <meta name="msapplication-config" content="/browserconfig.xml" />
    <meta name="msapplication-TileColor" content="#1F2A1B" />

    <!-- Twitter card extra -->
    <meta name="twitter:site" content="@casemobiliusate" />
    <meta name="twitter:creator" content="@casemobiliusate" />

    <!-- OG image dimensions (utili per Facebook/LinkedIn debugger) -->
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:alt" content={fullTitle} />

    <!-- Article meta (per og:type article) -->
    {ogType === 'article' && (
      <>
        <meta property="article:author" content="Andrea Bressan" />
        <meta property="article:publisher" content="https://www.casemobiliusate.com" />
      </>
    )}

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,500&display=swap" rel="stylesheet" />

    <SchemaOrganization />
    <slot name="head" />
  </head>
  <body>
    <a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:bg-white focus:px-3 focus:py-2 focus:rounded">Vai al contenuto</a>
    <Header />
    {showHero && <PillarHero pillarSlug={pillarSlug} />}
    <main id="main">
      <slot />
    </main>
    <Footer />
  </body>
</html>
